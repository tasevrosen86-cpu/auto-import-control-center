# The daemon is disabled so a build never leaves a stale JVM behind on the
# small build machine, and the heap is capped because the app itself is tiny.
org.gradle.daemon=false
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=false
android.nonTransitiveRClass=true
